<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <!-- <h4>PROFILE</h4> -->
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block mb-5" href="/logout">Logout</a>

        </div>
        <h5>Edit your post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea class="w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md mt-5" placeholder="What's in your mind" name="content" id="">
                <%= post.content%>
            </textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 block rounded-md mt-2 text-sm text-black" type="submit" value="Update Post" >
        </form>

    </div>
</body>
</html>