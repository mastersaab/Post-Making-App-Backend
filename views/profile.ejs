<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <!-- <h4>PROFILE</h4> -->
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block mb-5" href="/logout">Logout</a>
        </div>

        <div class="flex items-start gap-3 ">
            <div class="w-30 h-20 overflow-hidden">
                <img class="s-full h-full object-cover " src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl mb-5"><span class="font-light">Hello</span>  <%= user.name%>❤️</h3>

        </div>

        <h5>You can create a new post</h5>
        <form method="post" action="/post">
            <textarea class="w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md mt-5" placeholder="What's in your mind" name="content" id=""></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-2 text-sm" type="submit" value="Create New Post" >
        </form>
        <div class="posts mt-20  ">
            <h3 class="text-zinc-400"> Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 p-4 mb-4 border-2 bg-zinc-700 border-zinc-800 rounded-md">
                        <h4 class="text-blue-500 mb-2">@<%= user.username%></h4>
                        <p class="text-sm tracking-tight"><%= post.content%></p>
                        <small class="mt-2 inline-block"><%= post.likes.length%> likes</small>
                        <div class="btns flex gap-4 mt-1 ">
                            <a class="text-blue-500 " href="/like/<%= post._id%>">
                                <%=post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-yellow-400 ml-2" href="/edit/<%= post._id%>">Edit</a>
                        </div>
                    </div>
                <%}) %>
            </div>
        </div>
    </div>
</body>
</html>